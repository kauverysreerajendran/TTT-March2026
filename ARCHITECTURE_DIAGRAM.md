# Dashboard Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER BROWSER                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Dashboard UI (index.html)                                │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │ 📊 Dashboard Header (Title + Date)                 │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │ ┌─ Module 1: Day Planning ──────────────────────┐  │  │  │
│  │  │ │ [Total] [Progress] [Completed] [InProgress] │  │  │  │
│  │  │ └────────────────────────────────────────────┘  │  │  │
│  │  │ ┌─ Module 2: Input Screening ───────────────────┐ │  │  │
│  │  │ │ [Total] [Progress] [Completed] [InProgress] │ │  │  │
│  │  │ └────────────────────────────────────────────┘ │  │  │
│  │  │ ┌─ Module 3: Brass QC ──────────────────────────┐ │  │  │
│  │  │ │ [Total] [Progress] [Completed] [InProgress] │ │  │  │
│  │  │ └────────────────────────────────────────────┘ │  │  │
│  │  │ ┌─ Module 4: Brass Audit ───────────────────────┐ │  │  │
│  │  │ │ [Total] [Progress] [Completed] [InProgress] │ │  │  │
│  │  │ └────────────────────────────────────────────┘ │  │  │
│  │  │ ┌─ Module 5: IQF ───────────────────────────────┐ │  │  │
│  │  │ │ [Total] [Progress] [Completed] [InProgress] │ │  │  │
│  │  │ └────────────────────────────────────────────┘ │  │  │
│  │  │ ┌─ Module 6: Jig Loading ──────────────────────┐ │  │  │
│  │  │ │ [Total] [Progress] [Completed] [InProgress] │ │  │  │
│  │  │ └────────────────────────────────────────────┘ │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  Navigation Dots: ● ● ● ● ● ●                            │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↑ HTTP GET
                              │ /home/
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    DJANGO SERVER                                │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  adminportal/views.py - IndexView                        │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │ get(request):                                       │  │  │
│  │  │ 1. Check authentication                             │  │  │
│  │  │ 2. Get allowed_modules (user's permissions)         │  │  │
│  │  │ 3. Call get_dashboard_stats(allowed_modules)        │  │  │
│  │  │ 4. Build context with statistics                    │  │  │
│  │  │ 5. Render template with context                     │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                                                             │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │ get_dashboard_stats(allowed_modules):               │  │  │
│  │  │ ┌───────────────────────────────────────────────┐   │  │  │
│  │  │ │ For each module in allowed_modules:          │   │  │  │
│  │  │ │ 1. Pattern match module name                 │   │  │  │
│  │  │ │ 2. Query database for statistics             │   │  │  │
│  │  │ │    - Total lots: ModelMasterCreation.count() │   │  │  │
│  │  │ │    - Accepted: IP_Accepted_TrayScan.count()  │   │  │  │
│  │  │ │    - Rejected: IP_Rejected_TrayScan.count()  │   │  │  │
│  │  │ │ 3. Calculate progress percentage             │   │  │  │
│  │  │ │ 4. Get module color                          │   │  │  │
│  │  │ │ 5. Build stat dict                           │   │  │  │
│  │  │ │ 6. Add to stats list                         │   │  │  │
│  │  │ └───────────────────────────────────────────────┘   │  │  │
│  │  │ Return: [stat1, stat2, stat3, stat4, stat5, stat6]  │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                    ↓ Query
                    │
┌─────────────────────────────────────────────────────────────────┐
│                   DJANGO ORM                                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Model Imports:                                            │  │
│  │ • modelmasterapp.ModelMasterCreation (Total Lots)        │  │
│  │ • modelmasterapp.DPTrayId_History (Day Planning)         │  │
│  │ • InputScreening.IPTrayId (Input Screening)              │  │
│  │ • InputScreening.IP_Accepted_TrayScan (Accepted)         │  │
│  │ • Brass_QC.BrassTrayId (Brass QC)                        │  │
│  │ • BrassAudit.BrassAuditTrayId (Brass Audit)              │  │
│  │ • IQF.IQFTrayId (IQF)                                     │  │
│  │ ... and more module-specific models                      │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                    ↓ Query
                    │
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE (SQLite)                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Tables:                                                   │  │
│  │ ┌─────────────┐ ┌──────────────┐ ┌────────────────────┐  │  │
│  │ │ modelmaster │ │ inputscreen  │ │ brass_qc_trayid    │  │  │
│  │ │ creation    │ │ ing_iptrayid │ │                    │  │  │
│  │ │             │ │              │ │ • tray_id          │  │  │
│  │ │ • id        │ │ • id         │ │ • lot_id           │  │  │
│  │ │ • batch_id  │ │ • lot_id     │ │ • tray_quantity    │  │  │
│  │ │ • lot_id    │ │ • tray_id    │ │ • batch_id         │  │  │
│  │ │             │ │ • user_id    │ │                    │  │  │
│  │ └─────────────┘ └──────────────┘ └────────────────────┘  │  │
│  │                                                             │  │
│  │ More tables for each module's tray data, accepted scans,  │  │
│  │ rejected scans, rejection reasons, audit logs, etc.       │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Sequence

```
1. USER ACTION
   └─ Click /home/ or navigate to dashboard

2. REQUEST
   └─ GET /home/ HTTP/1.1

3. AUTHENTICATION CHECK
   └─ IndexView.get(request)
      ├─ Check if user is authenticated
      └─ If not, redirect to login

4. PERMISSION RESOLUTION
   └─ get_allowed_modules_for_user(user)
      ├─ If superuser: return all modules
      ├─ If admin group: return all modules
      └─ Else: return modules from UserModuleProvision

5. STATISTICS GENERATION
   └─ get_dashboard_stats(allowed_modules)
      └─ For each module:
         ├─ Pattern match module name to data sources
         ├─ Query database:
         │  ├─ Total lots: ModelMasterCreation.objects.count()
         │  ├─ Accepted: Module.Accepted_TrayScan.objects.count()
         │  └─ Rejected: Module.Rejected_TrayScan.objects.count()
         ├─ Calculate progress: (accepted / total) * 100
         ├─ Get color: get_module_color(module_name)
         └─ Return: {
                'module': name,
                'label': display_name,
                'total_lot': count,
                'progress': percentage,
                'completed': accepted_count,
                'moved_to_next': in_progress_count,
                'drafted': drafted_count,
                'color': hex_color
            }

6. TEMPLATE RENDERING
   └─ Render index.html with context
      ├─ Dashboard header with title and date
      └─ For each stat in dashboard_stats:
         ├─ Module card with colored header
         └─ 5 stat cards inside:
            ├─ Total Lots
            ├─ Progress (with bar)
            ├─ Completed
            ├─ In Progress
            └─ Drafted

7. CSS STYLING
   └─ Apply responsive design
      ├─ Colors from stat['color']
      ├─ Grid layout based on screen size
      └─ Animations and effects

8. JAVASCRIPT INTERACTION
   └─ Enable carousel navigation
      ├─ Click navigation dots
      ├─ Smooth scroll to modules
      └─ Update active dot

9. RESPONSE
   └─ Return HTML to browser

10. RENDERING
    └─ Browser displays dashboard
       ├─ CSS applied
       ├─ JavaScript enabled
       └─ User sees interactive dashboard
```

## Component Relationships

```
┌─ User Session ────────────────────────────────────┐
│                                                    │
│  ┌─ Permissions ────────────────────────────────┐ │
│  │ • is_superuser                               │ │
│  │ • is_staff                                   │ │
│  │ • groups (Admin)                             │ │
│  │ • department                                 │ │
│  └──────────────────────────────────────────────┘ │
│         │                                         │
│         ↓                                         │
│  ┌─ get_allowed_modules_for_user() ────────────┐ │
│  │ Returns: ['Module 1', 'Module 2', ...]      │ │
│  └──────────────────────────────────────────────┘ │
│         │                                         │
│         ↓                                         │
│  ┌─ get_dashboard_stats() ──────────────────────┐ │
│  │ For each module:                             │ │
│  │ • Pattern match to data sources              │ │
│  │ • Query database for counts                  │ │
│  │ • Calculate statistics                       │ │
│  │ Returns: [stat_dict_1, stat_dict_2, ...]    │ │
│  └──────────────────────────────────────────────┘ │
│         │                                         │
│         ↓                                         │
│  ┌─ Dashboard Template ──────────────────────────┐ │
│  │ Loops through stats and renders:             │ │
│  │ • Module header card (colored)               │ │
│  │ • 5 stat cards per module                    │ │
│  │ • Navigation carousel dots                   │ │
│  └──────────────────────────────────────────────┘ │
│         │                                         │
│         ↓                                         │
│  ┌─ CSS & JavaScript ────────────────────────────┐ │
│  │ • Responsive layout                          │ │
│  │ • Animations                                 │ │
│  │ • Interactive carousel                       │ │
│  └──────────────────────────────────────────────┘ │
│         │                                         │
│         ↓                                         │
│  ┌─ User Interface ──────────────────────────────┐ │
│  │ Interactive dashboard displayed              │ │
│  └──────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────┘
```

## File Structure

```
Watchcase Tracker Titan/
├── adminportal/
│   ├── views.py                    [MODIFIED - Added dashboard logic]
│   │   ├── IndexView
│   │   │   ├── get()
│   │   │   ├── get_dashboard_stats()
│   │   │   ├── get_model_count()
│   │   │   └── get_module_color()
│   │   └── ...other views...
│   ├── models.py
│   ├── urls.py
│   └── ...
├── static/
│   └── templates/
│       └── index.html              [MODIFIED - Complete redesign]
│           ├── CSS styles
│           ├── HTML structure
│           └── JavaScript interactivity
├── DASHBOARD_IMPLEMENTATION.md     [NEW]
├── DASHBOARD_COMPLETE_GUIDE.md     [NEW]
├── IMPLEMENTATION_SUMMARY.md       [NEW]
├── FINAL_CHECKLIST.md             [NEW]
└── ...other files unchanged...
```

---

This architecture provides:
✅ Clean separation of concerns (backend logic, frontend presentation)
✅ Secure access control (authentication + authorization)
✅ Efficient data retrieval (optimized queries)
✅ Responsive design (works on all devices)
✅ Maintainable code (clear structure, good documentation)
✅ Extensible system (easily add new modules)
